# ECC Demo Page

Static GitHub Pages demo for:

**Neural Speech Compression: A Benchmark and A New Entropy-Constrained Codec**

Open `index.html` locally or serve the repository root. The audio table loads files from
`audio/sample-*/` using the naming convention described in `audio/README.md`.
